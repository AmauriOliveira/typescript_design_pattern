<h4>Behavioral Strategy</h4>

<p>
  O padrão de projeto Strategy cria uma família de algoritmos que podem ser intercambiados, ou seja,
  utilizar um algoritmo no lugar de outro, conforme a necessidade de quem os utiliza.
</p>

<h5>Result</h5>
<img src="../patterns/behavioral/strategyBefore.min.svg" alt="UML Before" />
<% for (const [key, value] of Object.entries(before)) { %>
<p><%= key %>: <%= value %></p>
<%} %>

<h5>After</h5>
<img src="../patterns/behavioral/strategyAfter.min.svg" alt="UML After" />
<% for (const [key, value] of Object.entries(after)) { %>
<p><%= key %>: <%= value %></p>
<%} %>
